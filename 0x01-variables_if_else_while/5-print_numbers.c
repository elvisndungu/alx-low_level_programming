#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - print alphabets in upper case and lower case
 * Return: 0
 */
int main(void)
{
	int ch;

	for (ch = 0; ch < 10; ch++)
	{
		printf(ch);
	}
	putchar('\n');

	return (0);
}
