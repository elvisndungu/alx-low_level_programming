#include "main.h"

/**
 * _abs - gets absolute value of an int
 * @n: gets a number to be evaluated
 * Return: Always 0 if non smol letter
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (n * -1);
	}
}
